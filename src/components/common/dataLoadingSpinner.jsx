import { ClipLoader } from "react-spinners";

export default function DataLoadingSpinner() {
  return (
    <>
        <ClipLoader
        color="#5383E8"
        size={20}
        speedMultiplier={1}
        />
    </>
  );
};
